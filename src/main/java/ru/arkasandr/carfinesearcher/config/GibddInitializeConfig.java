package ru.arkasandr.carfinesearcher.config;import com.microsoft.playwright.Browser;import com.microsoft.playwright.BrowserContext;import com.microsoft.playwright.BrowserType;import com.microsoft.playwright.Page;import lombok.RequiredArgsConstructor;import lombok.extern.slf4j.Slf4j;import org.apache.commons.io.FileUtils;import org.springframework.beans.factory.annotation.Value;import org.springframework.context.annotation.Configuration;import java.io.File;import java.nio.file.Paths;import java.util.ArrayList;import java.util.Arrays;import java.util.List;import static java.util.Arrays.asList;@Slf4j@RequiredArgsConstructor@Configurationpublic class GibddInitializeConfig {    private  static final String[] ARGUMENTS = {            "--no-sandbox",            "--disable-setuid-sandbox",            "--disable-dev-shm-usage",            "--disable-accelerated-2d-canvas",            "--no-first-run",            "--no-zygote",            "--disable-gpu"};    private static final Integer BROWSER_WIDTH = 1400;    private static final Integer BROWSER_HEIGHT = 1200;    private final BrowserInitializeConfig browserInitialize;    public BrowserContext getContext() {        List<String> arguments = new ArrayList<>(asList(ARGUMENTS));        Browser browser = browserInitialize.launchBrowser(new BrowserType.LaunchOptions().setHeadless(false)             //   .setArgs(arguments)  //true not work on Win7        );        return browserInitialize.newBrowserContext(browser,                new Browser.NewContextOptions().setViewportSize(BROWSER_WIDTH, BROWSER_HEIGHT));    }}